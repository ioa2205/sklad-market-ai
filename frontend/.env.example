# This is the AI-enabled distribution. Set to false only as an emergency/staged-rollout kill switch.
VITE_FEATURE_AI_AGENT=true

# Local Vite proxy target. The browser still calls relative /api/v1 URLs.
VITE_API_PROXY_TARGET=http://localhost:8080
