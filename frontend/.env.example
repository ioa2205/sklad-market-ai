# AI agent (Phase 3+). Anything but "true" means OFF (page keeps its current mock behavior).
VITE_FEATURE_AI_AGENT=false

# Local Vite proxy target. The browser still calls relative /api/v1 URLs.
VITE_API_PROXY_TARGET=http://localhost:8080
